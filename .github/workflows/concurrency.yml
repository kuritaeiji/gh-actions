name: Concurrency
on: workflow_dispatch
defaults:
  run:
    shell: bash
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  sleep:
    name: sleep
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: sleep
        run: sleep 120
